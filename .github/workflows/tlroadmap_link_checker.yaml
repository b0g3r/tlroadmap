name: Check links

#on:
#  schedule:
#    - cron:  '0 0 * * 1'
on: [push]

jobs:
  check-links:
    name: Check links
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: '13.x'

      - name: Cache dependencies
        uses: actions/cache@v1
        with:
          path: ~/.cache/yarn
          key: ${{ runner.os }}-yarn-${{ hashFiles(format('{0}{1}', github.workspace, '/yarn.lock')) }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install js dependencies
        run: yarn install --frozen-lockfile

      - name: Build vuepress site
        run: yarn run site:build

      - name: Check links
        uses: chabad360/htmlproofer@master
        with:
          directory: "./tlroadmap/.vuepress/dist"
          arguments: >-
            --assume-extension
            --typhoeus-config '{"timeout": 60}'
            --url-ignore "/www\.consultant\.ru/.*/"
#          "/www.consultant.ru/"
#          "/assets/deeper.jpg"
#          "boss.blogs.nytimes.com"
#          "blog.unusual-concepts.ru" # timeout
#          "/assets/test-pyramid.png"
#          "ttps://techradar.avito.ru/" # timeout